[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "selective-dta-b"
version = "0.1.0"
description = "Selective DTA B experiment workspace"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
